[Unit]
Description=Snapshot kitty windows (workspace + cwd + running program) for reboot restore
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=oneshot
ExecStart=%h/.config/kitty/session-save.py
